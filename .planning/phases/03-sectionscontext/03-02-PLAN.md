---
phase: 03-sectionscontext
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/App.jsx
  - frontend/src/i18n/locales/en/common.json
  - frontend/src/i18n/locales/de/common.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "SectionsProvider wraps inside ProtectedRoute in App.jsx"
    - "SectionsProvider is below AuthProvider in the component tree"
    - "i18n strings for loading error and retry exist in both EN and DE"
    - "No page makes standalone /api/sections fetch outside SectionsContext"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "SectionsProvider integration"
      contains: "SectionsProvider"
    - path: "frontend/src/i18n/locales/en/common.json"
      provides: "English translations for sections error states"
      contains: "sections.loadError"
    - path: "frontend/src/i18n/locales/de/common.json"
      provides: "German translations for sections error states"
      contains: "sections.loadError"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "frontend/src/context/SectionsContext.jsx"
      via: "import { SectionsProvider }"
      pattern: "SectionsProvider"
---

<objective>
Wire SectionsProvider into App.jsx, add i18n strings, and verify the full integration works.

Purpose: Without this wiring, the context exists but is not mounted in the component tree. This plan completes the Phase 3 integration.
Output: Updated App.jsx with SectionsProvider, updated EN/DE translation files, smoke verification.
</objective>

<execution_context>
@/Users/kevinbrauer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevinbrauer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-sectionscontext/03-CONTEXT.md
@.planning/phases/03-sectionscontext/03-UI-SPEC.md
@.planning/phases/03-sectionscontext/03-01-SUMMARY.md

<interfaces>
From frontend/src/context/SectionsContext.jsx (created in plan 01):
```js
export const SectionsContext = createContext(null);
export function SectionsProvider({ children }) { ... }
```

Current App.jsx provider nesting (top to bottom):
```
LanguageProvider > AuthProvider > CurrencyProvider > BrowserRouter > ... > ProtectedRoute > MainLayout
```

SectionsProvider MUST go inside ProtectedRoute, wrapping MainLayout. It uses useAuth() internally so it must be below AuthProvider.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SectionsProvider into App.jsx and add i18n strings</name>
  <files>frontend/src/App.jsx, frontend/src/i18n/locales/en/common.json, frontend/src/i18n/locales/de/common.json</files>
  <read_first>
    - frontend/src/App.jsx (current provider nesting and import structure)
    - frontend/src/i18n/locales/en/common.json (current translation keys structure)
    - frontend/src/i18n/locales/de/common.json (current translation keys structure)
    - frontend/src/context/SectionsContext.jsx (confirm export name)
  </read_first>
  <action>
**App.jsx changes:**

1. Add import at top: `import { SectionsProvider } from './context/SectionsContext';`
2. Wrap `<MainLayout />` inside `<SectionsProvider>` within the ProtectedRoute element prop. The result:
```jsx
<Route
  element={
    <ProtectedRoute>
      <SectionsProvider>
        <MainLayout />
      </SectionsProvider>
    </ProtectedRoute>
  }
>
```

This places SectionsProvider below AuthProvider (which is at app root) and inside ProtectedRoute (so it only fetches when authenticated).

**EN translations** — add a `"sections"` key at root level of `frontend/src/i18n/locales/en/common.json`:
```json
"sections": {
  "loadError": "Failed to load sections.",
  "loadErrorHint": "Check your connection and try again.",
  "retry": "Retry"
}
```

**DE translations** — add a `"sections"` key at root level of `frontend/src/i18n/locales/de/common.json`:
```json
"sections": {
  "loadError": "Abschnitte konnten nicht geladen werden.",
  "loadErrorHint": "Prufen Sie Ihre Verbindung und versuchen Sie es erneut.",
  "retry": "Erneut versuchen"
}
```
  </action>
  <verify>
    <automated>grep -q "SectionsProvider" frontend/src/App.jsx && grep -q "sections" frontend/src/i18n/locales/en/common.json && grep -q "sections" frontend/src/i18n/locales/de/common.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx contains `import { SectionsProvider } from './context/SectionsContext'`
    - App.jsx contains `<SectionsProvider>` wrapping `<MainLayout />`
    - App.jsx has SectionsProvider inside ProtectedRoute (not outside it)
    - en/common.json contains key `"loadError": "Failed to load sections."`
    - en/common.json contains key `"retry": "Retry"`
    - de/common.json contains key `"loadError": "Abschnitte konnten nicht geladen werden."`
    - de/common.json contains key `"retry": "Erneut versuchen"`
  </acceptance_criteria>
  <done>SectionsProvider is wired into the component tree at the correct position, and all Phase 3 i18n strings exist in both EN and DE</done>
</task>

<task type="auto">
  <name>Task 2: Verify no standalone /api/sections fetch exists outside context</name>
  <files>frontend/src/context/SectionsContext.jsx</files>
  <read_first>
    - frontend/src/context/SectionsContext.jsx
  </read_first>
  <action>
Run a grep across the frontend source to confirm no page or component makes a direct `/api/sections` fetch outside of SectionsContext.jsx and sectionsService.js. This validates Phase 3 success criterion #3.

Search for any of these patterns in `frontend/src/` excluding SectionsContext.jsx and sectionsService.js:
- `api.get('/api/sections` or `api.get("/api/sections`
- `fetch.*sections` patterns
- Direct imports of api.js that call sections endpoints

If any matches found outside the two allowed files, document them as issues. Expected result: zero matches.
  </action>
  <verify>
    <automated>count=$(grep -r "api\.\(get\|post\|put\|delete\).*['\"/]api/sections" frontend/src/ --include="*.js" --include="*.jsx" | grep -v "sectionsService.js" | grep -v "SectionsContext.jsx" | wc -l); test "$count" -eq 0 && echo "PASS: no standalone sections fetch" || echo "FAIL: $count standalone fetches found"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `api.(get|post|put|delete).*api/sections` in frontend/src/ excluding sectionsService.js and SectionsContext.jsx returns 0 matches
  </acceptance_criteria>
  <done>No frontend page or component makes standalone /api/sections calls outside the context layer</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke test SectionsContext integration</name>
  <files>frontend/src/App.jsx</files>
  <action>
Human verifies the complete SectionsContext integration works end-to-end in the browser.
  </action>
  <what-built>Complete SectionsContext infrastructure: service layer (12 API functions), context provider with auth-scoped fetching, mutations (server-wait + optimistic), helper lookups, loading spinner, error banner with i18n, and App.jsx integration</what-built>
  <how-to-verify>
    1. Start the frontend dev server: `cd frontend && npm run dev`
    2. Open the app in browser and log in
    3. Verify the app loads normally (sections fetch succeeds, no spinner stuck)
    4. Open browser DevTools Network tab, filter for "sections" — confirm exactly one GET /api/sections request on page load
    5. Open React DevTools (if available) and confirm SectionsContext.Provider is in the component tree inside ProtectedRoute
    6. To test error state: temporarily change the API URL in sectionsService.js to an invalid path, reload, and confirm the error banner shows "Failed to load sections." with a Retry button. Then revert the change.
  </how-to-verify>
  <verify>Human confirms app loads with sections context active and error state displays correctly</verify>
  <done>User approves the SectionsContext integration works correctly in the browser</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -q "SectionsProvider" frontend/src/App.jsx` exits 0
- `grep -q "loadError" frontend/src/i18n/locales/en/common.json` exits 0
- `grep -q "Erneut versuchen" frontend/src/i18n/locales/de/common.json` exits 0
- No standalone sections API calls outside context files
- Backend tests still pass: `cd backend && npx vitest run`
</verification>

<success_criteria>
- SectionsProvider is mounted in the React tree inside ProtectedRoute, below AuthProvider
- All 3 i18n strings (loadError, loadErrorHint, retry) exist in both EN and DE
- No page fetches /api/sections directly (only through useSections hook)
- App loads successfully with sections context providing data
</success_criteria>

<output>
After completion, create `.planning/phases/03-sectionscontext/03-02-SUMMARY.md`
</output>
