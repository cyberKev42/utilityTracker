---
phase: 08-data-entry-mobile
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/pages/AddEntry.jsx
  - frontend/src/pages/Dashboard.jsx
  - frontend/src/pages/Statistics.jsx
  - frontend/src/pages/Entries.jsx
  - frontend/src/components/settings/SectionEditDialog.jsx
  - frontend/src/components/settings/SectionCard.jsx
  - frontend/src/components/settings/MeterRow.jsx
autonomous: true
requirements: [ENTR-01, ENTR-02]

must_haves:
  truths:
    - "User types '1,5' in usage amount field and value is accepted and parsed as 1.5"
    - "User sees German section names (Wasser/Strom/Brennstoff) when German is active"
    - "User-created sections display their raw name regardless of language"
    - "Invalid numeric input shows red border and disables submit"
    - "Settings numeric inputs (if any) also accept comma decimal per D-05"
  artifacts:
    - path: "frontend/src/pages/AddEntry.jsx"
      provides: "Comma-aware numeric inputs + translated section dropdown"
      contains: "normalizeDecimal"
    - path: "frontend/src/pages/Dashboard.jsx"
      provides: "Translated section names on cards"
      contains: "getSectionDisplayName"
    - path: "frontend/src/pages/Statistics.jsx"
      provides: "Translated section names in headers"
      contains: "getSectionDisplayName"
    - path: "frontend/src/pages/Entries.jsx"
      provides: "Translated section names in list and filter"
      contains: "getSectionDisplayName"
  key_links:
    - from: "frontend/src/pages/AddEntry.jsx"
      to: "frontend/src/utils/normalizeDecimal.js"
      via: "import and call in validateField + handleSubmit"
      pattern: "normalizeDecimal"
    - from: "frontend/src/pages/AddEntry.jsx"
      to: "frontend/src/utils/sectionName.js"
      via: "import and call in section dropdown render"
      pattern: "getSectionDisplayName"
    - from: "frontend/src/pages/Dashboard.jsx"
      to: "frontend/src/utils/sectionName.js"
      via: "import and call for section card titles"
      pattern: "getSectionDisplayName"
    - from: "frontend/src/pages/Statistics.jsx"
      to: "frontend/src/utils/sectionName.js"
      via: "import and call for section headers"
      pattern: "getSectionDisplayName"
    - from: "frontend/src/pages/Entries.jsx"
      to: "frontend/src/utils/sectionName.js"
      via: "import and call for filter dropdown and list display"
      pattern: "getSectionDisplayName"
---

<objective>
Wire comma decimal normalization into all numeric inputs and section name translation into all section display points across the app.

Purpose: Fulfills ENTR-01 (comma decimal) and ENTR-02 (translated section names) using the utilities created in Plan 01.
Output: All numeric inputs accept commas; all section names display translated when language is German.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-data-entry-mobile/8-CONTEXT.md
@.planning/phases/08-data-entry-mobile/08-RESEARCH.md
@.planning/phases/08-data-entry-mobile/08-UI-SPEC.md
@.planning/phases/08-data-entry-mobile/08-01-SUMMARY.md

<interfaces>
From frontend/src/utils/normalizeDecimal.js (created in Plan 01):
  export function normalizeDecimal(value) — replaces ',' with '.' in string values

From frontend/src/utils/sectionName.js (created in Plan 01):
  export function getSectionDisplayName(section, t) — returns t(`sectionNames.${key}`) or section.name

From frontend/src/pages/AddEntry.jsx — parseFloat call sites that need normalizeDecimal:
  Line 163: validateField usageAmount — parseFloat(value)
  Line 169: validateField meterReading — parseFloat(value)
  Line 175: validateField unitPrice — parseFloat(value)
  Line 261: handleSubmit — parseFloat(meterReading)
  Line 263: handleSubmit — parseFloat(usageAmount)
  Line 267: handleSubmit — parseFloat(unitPrice)
  Line 463: type="number" on usage amount input
  Line 557: type="number" on meter reading input
  Line 583: type="number" on unit price input

Section name display points:
  Dashboard.jsx line 205: {section.name}
  Statistics.jsx line 203: {section.name}
  Entries.jsx line 222-223: {s.name} in dropdown option
  AddEntry.jsx line ~365: section dropdown renders s.name

Settings components (audit for D-05):
  SectionEditDialog.jsx: Input for section name (text, no parseFloat — no change needed)
  SectionEditDialog.jsx: Input for custom unit (text, no parseFloat — no change needed)
  MeterRow.jsx: Input for meter name rename (text, no parseFloat — no change needed)
  No numeric inputs with type="number" or parseFloat exist in Settings components.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comma decimal input in AddEntry.jsx</name>
  <files>frontend/src/pages/AddEntry.jsx</files>
  <read_first>
    frontend/src/pages/AddEntry.jsx,
    frontend/src/utils/normalizeDecimal.js,
    frontend/src/components/ui/input.jsx
  </read_first>
  <action>
Per D-01, D-02, D-03, D-04, D-05:

1. Add import at top: `import { normalizeDecimal } from '../utils/normalizeDecimal';`

2. In `validateField` function, wrap every `parseFloat(value)` call with `normalizeDecimal`:
   - Line ~163 (usageAmount): `const num = parseFloat(normalizeDecimal(value));`
   - Line ~169 (meterReading): `const num = parseFloat(normalizeDecimal(value));`
   - Line ~175 (unitPrice): `const num = parseFloat(normalizeDecimal(value));`

3. In `handleSubmit` function, wrap every `parseFloat()` call:
   - Line ~261: `payload.meter_reading = parseFloat(normalizeDecimal(meterReading));`
   - Line ~263: `payload.usage_amount = parseFloat(normalizeDecimal(usageAmount));`
   - Line ~267: `payload.unit_price = parseFloat(normalizeDecimal(unitPrice));`

4. In cost calculation logic (lines ~115, ~124-125), wrap parseFloat calls:
   - `const usage = parseFloat(normalizeDecimal(usageAmount));`
   - `const price = parseFloat(normalizeDecimal(unitPrice));`

5. Change all numeric `<Input>` elements from `type="number"` to `type="text"`:
   - Line ~463: usage amount input — change to `type="text"` `inputMode="decimal"`, remove `step` and `min` attributes if present
   - Line ~557: meter reading input — same change
   - Line ~583: unit price input — same change
   Keep `inputMode="decimal"` on all three (already present, just keep it).

6. Verify submit button disabled state: ensure the submit button has `disabled={Object.values(fieldErrors).some(Boolean)}` or equivalent logic that prevents submission when validation errors exist (D-04).

Do NOT normalize on onChange — keep user's comma visible in the field (per RESEARCH.md anti-pattern guidance). Normalization happens only at parse time in validateField and handleSubmit.
  </action>
  <verify>
    <automated>cd /Users/kevinbrauer/Documents/Projects/claude/utilityTracker && grep -c 'normalizeDecimal' frontend/src/pages/AddEntry.jsx && grep -c 'type="text"' frontend/src/pages/AddEntry.jsx && grep 'type="number"' frontend/src/pages/AddEntry.jsx; echo "should show 0 number types for amount fields"</automated>
  </verify>
  <done>All numeric inputs in AddEntry accept comma input. Validation and submission normalize comma to dot before parseFloat.</done>
</task>

<task type="auto">
  <name>Task 2: Section name translation across all pages + Settings D-05 audit</name>
  <files>
    frontend/src/pages/AddEntry.jsx,
    frontend/src/pages/Dashboard.jsx,
    frontend/src/pages/Statistics.jsx,
    frontend/src/pages/Entries.jsx,
    frontend/src/components/settings/SectionEditDialog.jsx,
    frontend/src/components/settings/SectionCard.jsx,
    frontend/src/components/settings/MeterRow.jsx
  </files>
  <read_first>
    frontend/src/pages/Dashboard.jsx,
    frontend/src/pages/Statistics.jsx,
    frontend/src/pages/Entries.jsx,
    frontend/src/pages/AddEntry.jsx,
    frontend/src/utils/sectionName.js,
    frontend/src/components/settings/SectionEditDialog.jsx,
    frontend/src/components/settings/SectionCard.jsx,
    frontend/src/components/settings/MeterRow.jsx
  </read_first>
  <action>
Per D-06 through D-10:

In each page file, add import: `import { getSectionDisplayName } from '../utils/sectionName';`
Ensure `useTranslation` is imported and `const { t } = useTranslation();` is available (already present in most pages).

**Dashboard.jsx:**
- Line ~205: `{section.name}` -> `{getSectionDisplayName(section, t)}`
- Line ~210: `aria-label={...section.name...}` -> use `getSectionDisplayName(section, t)` in the aria-label

**Statistics.jsx:**
- Line ~203: `{section.name}` -> `{getSectionDisplayName(section, t)}`

**Entries.jsx:**
- Line ~222-223: `{s.name}` in `<option>` -> `{getSectionDisplayName(s, t)}`

**AddEntry.jsx:**
- Find the section dropdown `<select>` or mapping that renders `s.name` (around line 365) and replace with `getSectionDisplayName(s, t)`

IMPORTANT: Do NOT change the `value` attribute of dropdown options — those must remain `s.name` or `s.id` (whatever the current value binding is). Only change the DISPLAY text. The `sectionLookup[s.name]` logic in Dashboard/Entries uses the raw name for matching — do not change lookup keys, only display output.

**Settings D-05 audit (per D-05: "every numeric input across the entire app"):**
Read SectionEditDialog.jsx, SectionCard.jsx, and MeterRow.jsx. Confirm:
- SectionEditDialog.jsx has only text inputs (section name, custom unit) — no numeric parsing, no change needed.
- SectionCard.jsx has no `<Input>` elements with numeric types — no change needed.
- MeterRow.jsx has only a text input for meter name rename — no change needed.
- If ANY `parseFloat` or `type="number"` is found in these files, apply the same `normalizeDecimal` + `type="text"` + `inputMode="decimal"` pattern.
- Document audit result in SUMMARY: "Settings components audited per D-05 — no numeric inputs found."
  </action>
  <verify>
    <automated>cd /Users/kevinbrauer/Documents/Projects/claude/utilityTracker && grep -l 'getSectionDisplayName' frontend/src/pages/Dashboard.jsx frontend/src/pages/Statistics.jsx frontend/src/pages/Entries.jsx frontend/src/pages/AddEntry.jsx && echo "--- D-05 audit ---" && grep -c 'parseFloat\|type="number"' frontend/src/components/settings/SectionEditDialog.jsx frontend/src/components/settings/SectionCard.jsx frontend/src/components/settings/MeterRow.jsx; echo "All should be 0"</automated>
  </verify>
  <done>All four pages display translated section names for default sections and raw names for user-created sections. Settings components audited per D-05 — no numeric inputs requiring normalizeDecimal found.</done>
</task>

</tasks>

<verification>
- Comma input: type="text" with inputMode="decimal" on all numeric fields in AddEntry
- Every parseFloat call in AddEntry wrapped with normalizeDecimal
- Section translation: getSectionDisplayName used in Dashboard, Statistics, Entries, AddEntry
- Lookup keys (sectionLookup, value attributes) unchanged — only display text translated
- No type="number" remaining on amount/price inputs
- Settings components audited for D-05 compliance — no numeric inputs found
</verification>

<success_criteria>
- User types "1,5" in usage amount field and it validates as a valid positive number
- User types "abc" and gets red border + disabled submit
- German language shows "Wasser", "Strom", "Brennstoff" in section dropdowns and page headers
- English language shows "Water", "Power", "Fuel"
- User-created sections show their raw name in both languages
- Settings audit confirms no numeric inputs need normalizeDecimal
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-entry-mobile/08-02-SUMMARY.md`
</output>
